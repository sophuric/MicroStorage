{
  "schemaVersion": 1,
  "id": "${mod_id}",
  "version": "${mod_version}",
  "name": "μStorage",
  "description": "Simple storage system mod",
  "authors": [
    "sophur"
  ],
  "contact": {
    "sources": "https://github.com/sophuric/MicroStorage"
  },
  "license": "MPL-2.0",
  "icon": "assets/microstorage/icon.png",
  "environment": "*",
  "entrypoints": {
    "client": [
      "me.sophur.microstorage.client.MicroStorageClient"
    ],
    "main": [
      "me.sophur.microstorage.MicroStorage"
    ]
  },
  "mixins": [
    "microstorage.mixins.json",
    {
      "config": "microstorage.client.mixins.json",
      "environment": "client"
    }
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader_version}",
    "fabric-api": ">=${fabric_api_version}",
    "minecraft": "~${minecraft_version}",
    "java": ">=21",
    "brrp_v1": ">=${brrp_version}"
  }
}
